no implement, be honest,
plan to get this .py to run on dataflow gcp with python via CLI command
step to do:

1. config file in config.py
2. pre-dataflow job is created dataflow flex template:
    2.1 metadata.json for metadata-file in build step of flex template
    2.2 create an Artifact Registry repository
    2.3 create a docker file to build the image from this directory
    2.4 build the image and push it to the Artifact Registry repository
    2.5 Build the flex template using 2.1 to 2.5 above
        example:
            ```sh
            export PROJECT="poc-piloturl-nonprod"
            export BUCKET="terra-mhesi-dp-poc-gcs-bronze-01/simplecsv"

                gcloud dataflow flex-template build gs://$BUCKET/simplecsv_py.json \
                    --image-gcr-path "$REGION-docker.pkg.dev/$PROJECT/$REPOSITORY/simplecsv-py:latest" \
                    --sdk-language "PYTHON" \
                    --flex-template-base-image "PYTHON3" \
                    --py-path "." \
                    --metadata-file "metadata.json" \
                    --env "FLEX_TEMPLATE_PYTHON_PY_FILE=simplecsv.py" \
                    --env "FLEX_TEMPLATE_PYTHON_REQUIREMENTS_FILE=requirements.txt"
            ```
2. create a dataflow job in dataflow console by run flex-template that created
3. monitor the job in dataflow job via log (CLI access)
